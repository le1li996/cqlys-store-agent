# 门店经营智脑 · 部署说明

本项目是「单进程全栈」结构：一个 Node(Express) 服务**同时**托管前端静态资源（`dist/`）和后端 API（`/api/*`）。
因此部署到任意能运行 Node 的环境即可，无需再单独配 Nginx 反代或对象存储（生产建议见文末）。

---

## 一、目录结构（部署包）

```
.
├── server/
│   ├── index.mjs        # 后端服务（验证码/注册/登录/文件上传下载）
│   ├── db.json          # 运行期用户/文件索引（自动生成，已被 .gitignore 忽略）
│   └── uploads/          # 上传文件落盘（运行期生成）
├── src/                  # 前端源码（React + Vite）
├── public/               # 静态资源
├── dist/                 # 构建产物（npm start 时自动生成）
├── package.json          # 含 start / prestart 脚本
├── vite.config.js
├── index.html
├── Procfile             # PaaS 识别：web: node server/index.mjs
├── .env.example         # 后端环境变量模板
└── 部署说明.md
```

---

## 二、本地运行（开发 / 演示）

```bash
# 1) 安装依赖
npm install

# 2) 启动后端（默认 8787）
npm run dev:server        # 若已加该脚本；否则：node server/index.mjs
# 或另开终端跑前端开发服务器（5173，/api 经 vite proxy 转发到 8787）
npm run dev

# 3) 生产形态：构建前端并由后端同进程托管
npm start                 # prestart 会自动 vite build，然后 node server/index.mjs
# 浏览器打开 http://localhost:8787
```

---

## 三、部署到 Node 平台（以 Render 为例）

1. 把项目推到 Git 仓库（已含 `.gitignore`，不会带 `node_modules`/`dist`/运行时数据）。
2. 在 Render 新建 **Web Service**，关联仓库，配置：
   - **Build Command**：`npm install`（或留空由平台默认）
   - **Start Command**：`npm start`（平台会先跑 `prestart` 构建 dist，再启动后端）
   - **Instance Type**：免费版即可起步
3. 在 Render 的 **Environment** 中添加环境变量（见第四节），至少设 `SMS_PROVIDER=dev` 先跑通流程。
4. 部署完成后，Render 给出公网地址（如 `https://xxx.onrender.com`），它就是「可公开访问的完整站点」——
   注册、登录、文件上传下载全部可用。

> Heroku / Railway / 任意 PaaS 同理：`npm install` + `npm start`，平台注入 `PORT`。
> 纯服务器（VM / docker）：`npm install && npm start`，前面挂一层反代（Caddy/Nginx）做 HTTPS 与域名即可。

---

## 四、环境变量清单

| 变量 | 说明 | 默认 |
|---|---|---|
| `PORT` | 服务端口 | `8787`（平台通常会自动注入） |
| `SMS_PROVIDER` | `dev` 回显验证码 / `tencent` 真实下发 | `dev` |
| `TENCENT_SECRET_ID` | 腾讯云 SecretId | 空 |
| `TENCENT_SECRET_KEY` | 腾讯云 SecretKey | 空 |
| `TENCENT_SMS_APP_ID` | 短信 SdkAppId | 空 |
| `TENCENT_SIGN_NAME` | 短信签名 | 空 |
| `TENCENT_TEMPLATE_ID` | 验证码模板 ID | 空 |
| `TENCENT_REGION` | 地域 | `ap-guangzhou` |

短信默认 `dev` 模式：验证码打印到后端日志，便于本地/演示。
要真实下发：设 `SMS_PROVIDER=tencent` 并填齐 `TENCENT_*`，**代码无需改动**。

---

## 五、生产化建议（重要）

当前存储为**本地磁盘 + JSON 文件**（`server/db.json` + `server/uploads/`），适合原型与中小验证。
若上生产，请注意：

1. **临时文件系统**：Render 免费版等平台的磁盘是临时性的，重启/重新部署后 `db.json` 与 `uploads/` 会丢失。
   解决：使用**持久磁盘**（Render Disk / 挂载卷），或把存储换成数据库 + 对象存储。
2. **数据库**：把 `server/index.mjs` 底部的 `readDB/writeDB` 替换为 SQLite/Postgres（仅改这两个函数，路由不变）。
3. **文件存储**：上传文件改存对象存储（如腾讯云 COS / S3），`/api/files/:id` 改为重定向/签名 URL。
4. **HTTPS / 域名**：用平台自带的 HTTPS 或自挂 Caddy/Nginx；如需 `cqlys.ai` 自有域名，配 DNS + 证书后反代到本服务即可（`base` 仍为 `/`，无需改前端）。

---

## 六、健康检查

部署后可用以下端点自检：

```
GET /api/health   →  {"ok":true,"sms":"dev"}
```

返回 `200` 即服务正常；`sms` 字段显示当前短信通道。

---

## 七、切换前端「真实 / 本地演示」模式

前端数据层由 `VITE_API_MODE` 控制（构建时注入，见 `.env`）：

- `VITE_API_MODE=real`：注册/登录/文件走真实后端 `/api`（**部署包默认**）。
- 其他值：全部走 localStorage 兜底（无后端也不崩，但注册/上传为模拟）。

部署包已设为 `real`，并依赖后端同进程托管，无需额外配置。
